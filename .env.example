# Copy this file to .env and update values.
# Option A: use a single DATABASE_URL
# DATABASE_URL=postgresql://postgres:your_password@localhost:5432/ecopackai

# Option B: use separate DB_* variables
DB_HOST=localhost
DB_PORT=5432
DB_NAME=ecopackai
DB_USER=postgres
DB_PASSWORD=your_password
